{
  "name": "systems-starter-kit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Creative Systems Lab — writes ten Claude prompts for the visitor's actual business, gated on email.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test netlify/lib/*.test.mts",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.75.0",
    "@netlify/blobs": "^10.0.0",
    "@netlify/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
